#!/bin/bash

# Остановка скрипта при любой ошибке
set -e

echo "🚀 Начинаем настройку сервера для Polymarket Bota..."

# 1. Обновление системы
echo "📦 Обновление системных пакетов (может занять время)..."
sudo apt update && sudo apt upgrade -y

# 2. Установка системных зависимостей
echo "🔧 Установка Python3, pip, venv и screen..."
sudo apt install -y python3 python3-pip python3-venv git screen

# 3. Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "🐍 Создаем виртуальное окружение Python (venv)..."
    python3 -m venv venv
else
    echo "✅ Виртуальное окружение уже существует."
fi

# 4. Активация окружения и установка библиотек
echo "📥 Установка Python-зависимостей в venv..."
source venv/bin/activate

# Обновляем pip до последней версии
pip install --upgrade pip

# Устанавливаем библиотеки
# py-clob-client - для работы с Polymarket
# python-dotenv - для чтения .env файла
# numpy, scipy - для математики (Z-Score)
# requests, websockets - дополнительные полезные библиотеки
pip install py-clob-client python-dotenv numpy scipy requests websockets

echo "✅ Установка завершена успешно!"
echo "👉 Чтобы начать работу, активируй окружение: source venv/bin/activate"
echo "👉 Не забудь создать файл .env с твоими ключами!"
